# M0 acceptance — public plan and contracts

- **Milestone:** M0
- **Accepted source commit:** `b07694a7473f14a0b217b603a51d8c3d2b9cfa22`
- **Date:** 2026-08-02
- **URL:** https://github.com/PaulKinlan/wasm-vs-js
- **Review:** ACCEPT; no blocker or high finding
- **Reviewer run:** `9d6b26c9`

## User-visible result

The public repository exposes its README, canonical plan, task graph, repository rules, benchmark definition schema, immutable run schema, and contract checks. A reader can inspect the fairness tracks, lifecycle, correctness gate, sampling and stopping policy, provenance, browser evidence boundaries, Deno reporting architecture, milestone sequence, and definition of done.

## Verification

```sh
deno task check
```

The check passes formatting and lint, parses both closed JSON Schema 2020-12 contracts, and runs positive fixtures plus 15 negative invariants. Those invariants cover missing/duplicate variants, track consistency, optimized-baseline/log requirements, correctness and timing separation, work counters, exclusions, metric availability, and URI/date provenance.

## Corrections before acceptance

Independent reviews rejected earlier revisions for an inverted stopping rule, weak schema enforcement, an undefined canonical hash, inadequate provenance, missing same-target optimized baselines, and disabled URI/date validation. The accepted commit fixes those issues and specifies RFC 8785 canonicalization.

## Excluded claims

M0 provides no benchmark implementation, timing result, browser run, public dashboard, reporting API, Deno application, KV database, deployment, or JavaScript-versus-Wasm performance conclusion.

## Next journey

M1 builds one complete controlled JavaScript/linear-Wasm workload from reproducible build through correctness, cold/warm paired browser runs, raw evidence, statistics, and an inspectable local results page.
