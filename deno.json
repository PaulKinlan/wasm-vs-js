{
  "imports": {
    "ajv-formats": "npm:ajv-formats@3.0.1",
    "ajv2020": "npm:ajv@8.17.1/dist/2020.js",
    "wabt": "npm:wabt@1.0.37"
  },
  "tasks": {
    "build": "deno run --allow-read=. --allow-write=public/artifacts scripts/build.ts",
    "local": "deno run --allow-env=PORT --allow-net=127.0.0.1 --allow-read=. --allow-write=raw/runs server.ts",
    "summary": "deno run --allow-read=. --allow-write=raw/summaries scripts/summarize.ts",
    "test": "deno test --allow-env=PORT --allow-read --allow-write --allow-run",
    "typecheck": "deno check server.ts lib/*.ts scripts/*.ts tests/*.ts public/*.js benchmarks/sum-u32/workload.js",
    "check": "deno task build && deno fmt --check && deno lint && deno task typecheck && deno run --allow-read=. scripts/check-planning.mjs && deno run --allow-read=. scripts/check-contracts.mjs && deno task test"
  },
  "fmt": {
    "exclude": ["public/artifacts/"],
    "lineWidth": 100,
    "proseWrap": "preserve"
  },
  "lint": {
    "include": ["benchmarks/", "lib/", "public/", "scripts/", "server.ts", "tests/"]
  }
}
